# Frontend completion gate

Session hydration uses `/v1/me` after login and on application bootstrap. Navigation affordances are derived from the authenticated principal roles. Backend authorization remains authoritative.
